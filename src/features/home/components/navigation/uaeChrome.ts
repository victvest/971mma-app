/** Official UAE flag palette — the only color constraints for this chrome layer. */
export const UAE = {
  red: '#FF0000',
  green: '#00732F',
  white: '#FFFFFF',
  black: '#000000',
  ink: '#111111',
  inkMuted: 'rgba(17, 17, 17, 0.62)',
} as const;

export const NAV_CHROME = {
  clusterHeight: 58,
  iconSize: 30,
  avatarSize: 48,
  horizontalInset: 16,
  topInset: 8,
  glassRadius: 29,
  containerSpacing: 10,
  badgeSize: 10,
} as const;

# DEPRECATED as a separate secrets file.
#
# All local secrets live in the app root:
#   971mma-app/.env
#
# Create / refresh the symlink (from this directory):
#   ln -sf ../.env .env.local
#
# Full catalog + security rules: ../.env.example
